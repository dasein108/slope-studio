"""marketing-guru — closed-loop viral growth engine.

A persistent per-channel JOURNAL drives a feedback loop:
    ideate (bet) -> deploy (film-maker) -> measure (virality) -> learn -> ideate

Each journal entry is a falsifiable bet: an idea + hook + the assumption behind it
+ the goal metric. After deploying ~10 videos we can score virality RELATIVE to the
channel's own portfolio, confirm/refute assumptions, and steer the next idea.
"""

from __future__ import annotations
